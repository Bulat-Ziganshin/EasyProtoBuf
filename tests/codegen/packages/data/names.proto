syntax = "proto2";
package foo.bar;

enum State {
  READY = 7;
}

message Outer {
  message Inner {
    optional int32 value = 1;
  }

  optional Inner child = 1;
  optional State state = 2 [default = READY];
}
